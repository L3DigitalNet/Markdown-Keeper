# Project Status

## Current snapshot

- Markdown-Keeper `v1.0.0` is the stable release; compatibility is frozen for the `1.x` line.
- The service provides SQLite-backed Markdown ingest, lexical and semantic search, CLI, JSON-RPC, and durable watcher events.
- The 174-test dependency-light suite passes; seven semantic-search integration tests require the optional ML stack.
- Project Standards 5.11.0 Catalog 5 manages Agent Handoff 1.6 with the preserved automatic Claude/Codex profile and canonical `docs/` knowledge paths.
- Reconciliation, validation, and the platform-interpreter hook check pass; automatic startup under the Python shim remains blocked by upstream issue #80.
