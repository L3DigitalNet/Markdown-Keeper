# Sessions — 2026-05

| Date | Summary |
|---|---|
| 2026-05-31 | Re-orientation after long absence. Found `state.md`/`deployed.md` were placeholders; populated them with real status (0.9.5 done, 1.0.0 pending; `pyproject` still 0.1.0; 181 tests). Reconciled `main` ↔ `testing` divergence: ported stranded doc work (architecture/usage/readme) from `testing` onto `main` (`b3e2895`), fixed broken uppercase `ARCHITECTURE.md` link + stale test count, then merge-resynced `testing` to match `main` (`0f0e72d`) without force-push (guard-compliant). Recovery tag `testing-pre-resync`. |
| 2026-05-31 | **Released v1.0.0** (`8b17b6b`, tag `v1.0.0`). Built local `.venv` (Py3.13, CPU torch + ML extras) to run the ML-gated integration suite. KPIs verified on real MiniLM model: precision@5 1.000, p95 0.3 ms, 25 docs/13.5 s. Fixed 3 brittle unit tests that asserted the hash fallback and broke once ML deps were installed (forced fallback at the `_load_model` seam; `a65ce75`) — suite green with/without ML (174 unit + 7 integration). Froze `docs/COMPATIBILITY.md` for `1.x`, wrote `CHANGELOG.md`, bumped version 0.1.0→1.0.0 (pyproject + `__version__`). GitHub Release not yet created. |
